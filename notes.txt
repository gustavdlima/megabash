
ls | wc -l | echo oi | env | ls < eof wc -l > b
ls | wc -l | echo oi | env | ls << eof wc -l > file		(Cria file, abrindo o prompt, dá leak <- cria file)
ls | wc -l | echo oi | env | ls > file << eof wc -l		(cria file, mas não mostra prompt)
ls | wc -l | echo oi | env | ls > file << eof wc -l		(cria file, mas ela tem o valor de wc -l, quando deveria ser vazia)
ls | wc -l | echo oi | env | ls > file << eof ls

ls >> a >> b
wc -l < a > b

aparentemente depois de um redirecionamento ele ignora os outros

heredoc

ele não abre o prompt quando é o primeiro comando dos pipes ou está entre pipes, pq?

dá leak na env quando ele é chamado sozinho, porem com mais comandos ele tá normal, porém se tentar tirar o leak pro caso dele estar sozinho, ele dá invalid read of size da env para os proximos comandos exceto quando é o próprio heredoc sozinho.


.:: PARADINHAS PRA RESOLVER ::.

3. $? : arrumar saídas

7. Heredoc is not opening when is called

8. O ultimo redirecionamento tem prioridade! (input/heredoc)

9. >|< tem que aparecer bash: syntax error near unexpected token `<' por que?


16
